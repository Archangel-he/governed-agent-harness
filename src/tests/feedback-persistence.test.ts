import test from 'node:test';import assert from 'node:assert/strict';import {mkdtempSync} from 'node:fs';import {tmpdir} from 'node:os';import {join} from 'node:path';import {DynamicEvaluationStore} from '../governance/dynamic-evaluation.js';
test('dynamic feedback survives reopen and exposes latest state',()=>{const file=join(mkdtempSync(join(tmpdir(),'feedback-')),'events.jsonl');const a=new DynamicEvaluationStore('x','1',file);a.recordDecision({id:'d',input:1,output:2,decisionAt:1});a.recordFeedback({id:'f',caseId:'d',observedAt:2,value:3,source:'test'});const b=new DynamicEvaluationStore('x','1',file);assert.equal(b.list()[0].status,'scored');assert.equal(b.snapshot(3).cases[0].feedback?.value,3)});
